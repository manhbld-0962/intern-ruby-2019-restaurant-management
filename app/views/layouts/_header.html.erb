<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <%= link_to t(:name_restaurant), root_path, id: "logo" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to t("header.home"), root_path %>
        </li>
        <li><%= link_to t("header.menu"), "#"%></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <%= link_to "#", class: "dropdown-toggle", data: { toggle: "dropdown" } do %>
            <%= current_user.name_user %>
            <b class="caret"></b><% end %>
            <ul class="dropdown-menu">
              <li><%= link_to t("header.profile"), edit_user_registration_path %></li>
              <li><%= link_to t("header.booking"), "#" %></li>
              <li class="divider"></li>
              <li><%= link_to t("devise.shares.logout"), destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <%= link_to t("devise.shares.signup"), sign_up_path %>
          <%= link_to t("devise.shares.login"), login_path %>
        <% end %>
      </ul>      
    </div>
  </div>
</nav>
